{
  "name": "slate-contracts",
  "version": "0.0.1",
  "description": "Shared TypeScript contracts for the Slate MCP Runtime and VS Code extension",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p ./",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
