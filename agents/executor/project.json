{
  "$schema": "https://raw.githubusercontent.com/nrwl/nx/main/packages/nx/schemas/project-schema.json",
  "name": "agents-executor",
  "projectType": "library",
  "root": "agents/executor",
  "sourceRoot": "agents/executor/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p agents/executor/tsconfig.lib.json && tsc-alias -p agents/executor/tsconfig.lib.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ESLINT_USE_FLAT_CONFIG=false eslint agents/executor/src --ext .ts"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run agents/executor --passWithNoTests"
      }
    }
  },
  "tags": ["scope:agent", "type:lib"]
}
