# Orchestrator

This folder defines how agents are invoked.

- `run_packets/` contains example run envelopes (origin, command, options, input).
- Agents and context models live in `tools/agents` and `context/protocol`.
